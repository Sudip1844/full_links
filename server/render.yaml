services:
  - type: web
    name: moviezone-server
    env: node
    plan: starter
    buildCommand: npm install --include=dev
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production